= form_for @skip_content_rule do |f|
  - if @skip_content_rule.errors.any?
    #error_explanation
      %h2= "#{pluralize(@skip_content_rule.errors.count, "error")} prohibited this skip_content_rule from being saved:"
      %ul
        - @skip_content_rule.errors.full_messages.each do |msg|
          %li= msg

  .actions
    = f.submit 'Save'
